# [TEST] cofacts-takedown GitHub App identity check

這是一次基礎設施測試,用來確認 `/takedown` skill 的 `scripts/create-pr.js` 是否能以 `cofacts-takedown[bot]` 這個 GitHub App 身份成功開 PR。

**不是真實的封鎖案件,沒有任何使用者被調查或處置。** 這個 PR 會在確認作者身份後由發起測試的 agent 直接 close(不 merge)。
